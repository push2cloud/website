---
applications:
- name: push2cloud
  memory: 64M
  no-hostname: true
  domain: push2.cloud
  buildpack: https://github.com/cloudfoundry/staticfile-buildpack
  path: ./docs

  env:
    FORCE_HTTPS: true